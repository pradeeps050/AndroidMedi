<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.orangeskill.elate">


    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <dist:module dist:instant="true" />

    <application
        android:name=".framework.application.ElateApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">
        <activity android:name=".feature.feed.FeedActivity"
            android:theme="@style/NoActionBar"></activity>
        <activity
            android:name=".feature.video.VideoActivity"
            android:configChanges="orientation|keyboardHidden"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".feature.playlist.ui.therapy.PlayListActivity"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".feature.session.SessionActivity"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".feature.login.ui.login.LoginActivity"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".feature.otp.OTPActivity"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".feature.signup.SignUpActivity"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".feature.product.MainActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".feature.home.HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>